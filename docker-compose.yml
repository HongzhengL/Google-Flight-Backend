services:
  flight-backend:
    build: .
    container_name: flight-backend
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]
      interval: 60s
      timeout: 10s
      retries: 3
